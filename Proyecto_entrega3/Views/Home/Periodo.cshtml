@{
    ViewData["Title"] = "Periodo";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Periodo</h2>

@(Html.DevExtreme().DataGrid<Proyecto_entrega3.Models.Periodo>()
    .ShowBorders(true)
    .DataSource(d => d.WebApi().Controller("Periodo")
    .UpdateAction(true)
    .InsertAction(true)
    .DeleteAction(true)
    .Key("IdPeriodo"))
    .Columns(columns =>
    {
        columns.AddFor(m => m.IdPeriodo);
        columns.AddFor(m => m.NumeroPeriodo);
        columns.AddFor(m => m.InicioPeriodo);
        columns.AddFor(m => m.FinPeriodo);
        columns.AddFor(m => m.IdEstudiante)
        .Lookup(lookUp => lookUp
            .DataSource(
                d => d.WebApi()
                .Controller("Estudiante")
                .LoadAction("Get")
                .Key("IdEstudiante")
            )
            .ValueExpr("IdEstudiante")
            .DisplayExpr("Primernombre")
    );
    })
    .Editing(editing =>
    {
        editing.Mode(GridEditMode.Popup);
        editing.AllowUpdating(true);
        editing.AllowAdding(true);
        editing.AllowDeleting(true);
    })
    .Paging(p => p.PageSize(100))
    .FilterRow(f => f.Visible(true))
    .HeaderFilter(f => f.Visible(true))
    .GroupPanel(p => p.Visible(true))
    .Grouping(g => g.AutoExpandAll(false))
    .RemoteOperations(true)
)